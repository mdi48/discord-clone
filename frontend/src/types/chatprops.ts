interface ChatProps {
    channelId: string; // The ID of the channel to which the chat belongs
    // userId: string; // The ID of the user who is currently logged in
    // messages: Array<{
    //     id: string; // Unique identifier for the message
    //     content: string; // The text content of the message
    //     userId: string; // The ID of the user who sent the message
    //     createdAt: string; // Timestamp when the message was created
    // }>;
    // onSendMessage: (messageContent: string) => void; // Function to handle sending a new message
}
export default ChatProps;
